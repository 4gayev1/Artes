const {
  expect,
  Given,
  When,
  Then,
  context,
  element,
  selector,
  saveVar,
  extractVarsFromResponse,
  page,
  request,
  random,
  resolveVariable
} = require("./src/helper/imports/commons");
const {
  keyboard,
  mouse,
  frame,
  assert,
  elementInteractions,
  api,
} = require("./src/helper/stepFunctions/exporter");

module.exports = {
  expect,
  Given,
  When,
  Then,
  element,
  selector,
  saveVar,
  extractVarsFromResponse,
  resolveVariable,
  context,
  api,
  random,
  page,
  request,
  api,
  keyboard,
  mouse,
  frame,
  assert,
  elementInteractions,
};
